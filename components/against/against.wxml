<view class="drawer_screen" catchtap="closeShow" wx:if="{{showTag === true}}">
    <view class="drawer_box" catchtap="toFocus">
      <form catchsubmit="sendAgainst" style="width:100%;height:100%">
        <view class="texteare">
            <textarea name="content"  focus="{{focus}}" style="width:100%;height:100%"  placeholder="请输入举报信息" placeholder-class="placeholderclass"></textarea>
        </view>
        <button class="btn" style="width:65%;height:15%" form-type="submit">发布</button>
      </form>
    </view>
  </view>
